import React from 'react';

// TODO: Implement register page
// - Email + password (≥8 chars, uppercase + number) (SRS 4.1.3)
// - Google OAuth one-click
// - After submit: send verify email, show "check inbox" page
export default function RegisterPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-neutral-50">
      <div className="text-center">
        <h1 className="text-2xl font-semibold text-neutral-900">SBRB</h1>
        <p className="text-neutral-500 mt-2">Register page — to be implemented</p>
      </div>
    </div>
  );
}
